# 1939.py

import sys
input = sys.stdin.readline

def union_parent(a, b):
	a = find_parent
def find_parent(x):
	if graph[x] = x:
		return x
	graph[x][0] = find_parent(graph[x][0])
n, m = map(int, input().split())
graph = [[x, 0] for x in range(n + 1)]
# for i in range(m):
print(graph)